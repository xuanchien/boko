$("#bid-messages").append('<%= j render(@messages) %>');  

<% if @messages.next_page %>
  $(".pagination").unload();
  $(".pagination").replaceWith('<%=j will_paginate(@messages) %>');
<% else %>
  $(".pagination").remove();
<% end %>